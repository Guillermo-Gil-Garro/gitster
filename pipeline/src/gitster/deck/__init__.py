from gitster.deck.registry import (
    REGISTRY_COLUMNS,
    append_cards,
    delete_pending,
    load_registry,
    mark_printed,
    next_card_number,
    validate_registry,
)
from gitster.deck.selection import select_modular_deck
